# Rain Man — Blackjack Card Counting ML System
# Python 3.10+

# Core scientific computing
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7

# Terminal UI
rich>=13.0

# Screen capture (live mode)
mss>=9.0
Pillow>=10.0

# OCR card detection (live mode)
pytesseract>=0.3.10
opencv-python-headless>=4.8

# Hotkey listener (watch mode — single-key input)
pynput>=1.7
